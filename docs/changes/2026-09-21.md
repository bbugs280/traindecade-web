# 2026-09-21 — EN whey-vs-beef re-frame (evidence-validation, v3 foothold play)

**Profile:** build · **Trigger:** Daily Driver Today #3 (Traindecade DECIDE card) → Vincent chose
option ② "evidence-validation re-frame of the EN whey page" from the Stage 1 v3 honest menu.

**Rollback tag:** `rollback-pre-wheyreframe-2026-09-21` (HEAD `2061d88` before edits)
**Files:** `content/posts/whey-vs-beef-protein.md`, `content-zh/posts/whey-vs-beef-protein.md`

## Context — what the v3 pass asked for, and what was already done

The Stage 1 v3 foothold-first pass (queue updated 2026-09-21 14:59) identified EN
`/posts/whey-vs-beef-protein/` as the site's **largest impression pool: 61 impr, 0 clicks, pos 76.3**,
whose ZH twin converts at pos 5.3 / 20% CTR. The prescribed move was the **evidence-validation
re-frame**.

⚠️ **The intro-level re-frame was ALREADY SHIPPED (2026-09-17).** The page already carries the
"every competing article is written by someone selling a tub; this page sells nothing; every study
is named and linked" paragraph. Re-shipping that would have been a no-op commit.

**So the re-frame was executed as two genuinely-missing EVIDENCE additions instead** — verified absent
by grep before writing, both from primary sources:

1. **Protein-quality scores (PDCAAS).** Page cited collagen's risk qualitatively ("low in or missing
   tryptophan") but gave no score. Added: whey/milk isolate **1.00**, whole beef 0.92,
   **gelatin + collagen 0.00**; essential-AA per 100 g ≈ **35 g** (whey isolate) vs **19 g** (beef
   isolate, near-identical to gelatin).
2. **🔴 Study-funding + venue disclosure — the strongest addition, and the one that needed a
   correction.** The page's anchor study was **funded by Essentia Metabolic Proteins** — the
   manufacturer of the **BeefISO™** ingredient it tested. Live SERP check (2026-09-21) confirms
   **most competitor pages cite the 2015 conference abstract**, which carries no funding statement.

   ⚠️ **Build-tester caught a real sourcing error in the first draft of this commit** (see below).
   The initial version asserted the funding statement was *in* the cited `PMC4595383`, and told
   readers to verify it there. **`PMC4595383` is a conference ABSTRACT (JISSN 12(Suppl 1):P11)** —
   it has no funding section, and "Essentia" appears zero times on it. The linked page would not
   have corroborated the claim — exactly the failure mode an evidence-validation page exists to
   prevent. **Fixed** by re-sourcing to the 2018 full paper (**PMID 28399016**, *J Strength Cond
   Res* 32(8):2233–2242), whose published funding statement reads: *"Essentia Metabolic Proteins
   provided funding for this trial and all funding went to The University of Tampa."* The venue
   change (2015 abstract → 2018 full paper) is now stated in the copy itself, so the reader can see
   which version carries the declaration.

   ⚠️ **Framed honestly, not as a "gotcha":** the text explicitly notes the null result *favours
   whey* (the funder's rival), so this is disclosure, not a bias accusation. Directionally this
   strengthens rather than weakens the page's "beef is a legitimate tie" verdict. Also corrected:
   the 2018 trial tested **whey concentrate**, where beef, chicken and whey all came out even.

## Diff summary

```
EN + ZH, both twins edited in parallel (uniformity held):
  · +PDCAAS / essential-AA paragraph in "What each one actually is"
  · +Essentia funding-disclosure paragraph in the head-to-head section
  · +2 Sources entries (PDCAAS scores; funding disclosure w/ PMC4595383 link)
  · lastmod 2026-09-17T10:00:00+08:00 → 2026-09-21T15:00:00+08:00
```

No structural change: both files **87 → 98 lines**, identical H2 counts (7), no heading
renames, no new internal links, no title/description/tags change (title + description already carry
the target query set and are within budget).

⚠️ **Line count corrected from the first draft:** build-tester independently verified the count and
the final measured value is **87 → 98 lines** for both files (the first draft said 100). Also
undisclosed in the first draft and now on record: the ZH edit
included **three small rewordings of pre-existing ZH lines** (the "collagen is not a complete
protein" line, the "most important label check" line, and one sentence in `## 兩者究竟是什麼`) —
improvements to the Chinese, but they were shipped copy and are disclosed here rather than left
implicit. EN received no equivalent rewording, so the twins are **structurally** equivalent
(7 H2 each, same section order) rather than line-for-line paraphrase-equivalent.

## Verification

```
hugo --gc                        clean, 0 errors (253 EN / 267 ZH pages)
tests/test_multilingual.py       ✅ PASS — 34 EN / 34 ZH paired, hreflang bidirectional
tests/check_no_stray_emphasis.py ✅ PASS — 68 rendered posts, 0 stray markers

built output
  EN public/posts/whey-vs-beef-protein/index.html     Essentia ×3, PDCAAS ×3 ✓
  ZH public/zh/posts/whey-vs-beef-protein/index.html  Essentia ×3, PDCAAS ×3 ✓
  JSON-LD dateModified                                2026-09-21T15:00:00+08:00 ✓
  ZH language isolation                               no EN fallback phrases ✓
```

## ⚠️ Honest expectations (do not overclaim)

This does **not** fix EN position. "beef protein vs whey" is a commercial-intent SERP and the site is
~9 weeks old; the EN/ZH position gap is a **language-market fact, not a page defect** (confirmed
2026-09-17 — the two files were already structurally identical). The re-frame is the play that can
win citations/shares from readers who want a checkable page, not a copy fight. Position moves when
authority arrives.

## Not done (deliberately)

- **No new post.** The v3 gate found no new post survives foothold + dedupe — "write post #35" is not
  on the menu.
- **No backlinks.** Per the v3 note, EN backlinking remains a proven-unknown: no EN money query has
  demonstrated conversion.
- **No GainCut link added/changed** — the existing GainCut CTA is pre-existing and untouched.
